export const breakpointSize = {
  xs: "480px",
  md: "768px",
  lg: "1200px",
};

export const breakpointDevice = {
  xs: `(min-width: ${breakpointSize.xs})`,
  md: `(min-width: ${breakpointSize.md})`,
  lg: `(min-width: ${breakpointSize.lg})`
};
